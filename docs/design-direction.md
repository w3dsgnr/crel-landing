# Crel — дизайн-направление

Главный артефакт для созвона в четверг. Основа: `docs/refs-analysis.md` (анализ OnRamper /
Transak / логотипа) + `docs/brief.md` (итоги звонка). Решения помечены: **[решено]** — рабочая
рекомендация, можно строить; **[четверг]** — нужно слово заказчика.

> ## Статус слоёв
>
> | Слой | Состояние | Где описан |
> |---|---|---|
> | **v3 «Необанкинг»** | **🟢 рабочий** — строим по нему | «Слой v3» ниже |
> | v2 «Заряд» | 🗄 архив | «Цвет v2» (помечен) |
> | v2 «Сдержанный» | 🗄 архив | «Цвет v2 → Два слоя» |
>
> Что v3 **отменяет** из §1 и «Цвет v2»: палитру-раму, шкалу радиусов, правило теней/рамок,
> запрет разделителей-по-умолчанию, дозировку цвета «белая рама → цветные окна».
> Что v3 **наследует без изменений**: типографику (Space Grotesk, шкала), сетку 1200/12/24,
> вертикальный ритм 112/160, motion-словарь печати, зелёный вольтаж как ядро идентичности,
> §3 целиком (тумблер, «Перепечатка», URL-стратегия, доступность), запрет 3D-фигуратива.
>
> Токены в `app/globals.css` пока **не тронуты** — там живёт v2. Миграция кода — отдельный шаг
> после подтверждения v3 (порядок в «Слой v3 → Порядок миграции»).

---

## 1. Дизайн-токены сайта

База: язык логотипа c:rel_ (Space Grotesk, ч/б, острые углы, терминальная интонация) +
одобренная часть OnRamper (светлая витрина, крупная типографика, мокапы-фрагменты).
Лиловый подтон OnRamper (`#EFEEF3`) не берём — это их бренд-код; наши серые нейтральны.

### Палитра 🗄 **[архив — заменена палитрой v3]**

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#FFFFFF` | базовый фон (наследие crel.ch) |
| `--bg-alt` | `#F5F5F7` | чередование секций, светлые карточки |
| `--bg-deep` | `#ECECEF` | второй шаг чередования, подложки |
| `--surface` | `#FCFCFD` | подложки UI-мокапов (всегда светлее карточки под ними) |
| `--ink` | `#111111` | текст, заголовки, залитые кнопки (из crel.ch) |
| `--ink-soft` | `#6A6A70` | вторичный текст, неактивные состояния |
| `--line` | `#111111` @ 12–16% | хайрлайны, обводки 1px |
| `--grid` | `#BFBECA` | серебро cursor-grid декора hero (canvas-линии у курсора, не UI) |
| `--ink-invert` | `#FFFFFF` | текст на тёмном |
| `--accent` | `#00A344` | вольтаж-зелёный: глифы, линии, интерактив на светлом (3.3:1 — UI/крупный) |
| `--accent-deep` | `#067D3F` | мелкий текст статусов на светлом (5.2:1, AA) |
| `--accent-bright` | `#2BD576` | вольтаж на тёмных плоскостях (9.8:1 на `--ink`) |

Правило (ревизия «Цвет v2»): вольтаж и цвет живут в интерактиве, статусах, подложках **части**
карточек и финальном CTA — детали в разделе «Цвет v2» ниже. Плоскости-фоны сайта остаются
ахроматом; второй бренд-hue не вводим. Лиловая рама `#EFEEF3` и электрик-синий `#1F00FF`
OnRamper — чужой бренд-код, не берём.

### Типографика **[решено]**

Единственный шрифт — **Space Grotesk** (подтверждён кодом crel.ch). Не моноширинный:
геометрический гротеск с CLI-акцентами в деталях знаков.

| Роль | Размер / высота строки | Начертание | Трекинг |
|---|---|---|---|
| Display (hero) | clamp(56px → 112px) / 1.0 | 700 | −2% |
| H2 секции | 40–64px / 1.05 | 500–700 | −1.5% |
| Заголовок карточки | 24–28px / 1.15 | 500 | −1% |
| Body | 16–17px / 1.5 | 400 | 0 |
| Лейблы / статусы | 12–13px / 1.2, строчные | 400 | +8…+12% |

Иерархия — размером. Лейблы всегда строчные (капс запрещён: строчность — часть бренда,
логотип печатается строчными).

### Сетка и ритм **[решено]**

- Контейнер 1200px, 12 колонок, гэп 24px; поля ≥ 48px desktop / 20px mobile.
- Вертикаль секций: 120–160px; заголовок секции ↔ контент: 64px.
- Чередование фонов секций: `--bg` → `--bg-alt` → `--bg` → тёмный футер. Без линий-разделителей.

_Сетка и ритм переходят в v3 без изменений._

### Геометрия и глубина 🗄 **[архив — заменена «Пластикой v3»]**

Острая шкала радиусов — от кнопок заглушки crel.ch (3px), против 24px-мягкости OnRamper.
Рифмуется со Space Grotesk и швейцарской базой:

- `--r-s: 2px` — чипы, бейджи-статусы;
- `--r-m: 4px` — кнопки, поля, тумблер;
- `--r-l: 8px` — карточки;
- `--r-xl: 12px` — подложки UI-мокапов.

Тени: у карточек — нет (разделение тоном). Единственная тень в системе — у плавающих
UI-мокапов: `0 24px 64px rgba(17,17,17,0.10)` — правило OnRamper «интерфейс парит».
Систематизировано в «Цвет v2 → Вопрос 2»: тень — только парящим мокапам; плоские
и градиентные карты разделяются тоном / 1px-хайрлайном.

### Motion-токены **[решено]**

Весь словарь движения выводится из печати (см. принцип 7):

| Токен | Значение | Применение |
|---|---|---|
| `--t-char-brand` | 210ms/символ | фирменный TextType hero-логотипа (idle-цикл с crel.ch) |
| `--t-char-fast` | 40ms/символ | перепечатка при переключении направлений |
| `--t-blink` | 850ms, `step-start` | мигание курсора (жёсткое вкл/выкл, без фейда) |
| `--d-quick / -base / -page` | 200 / 400 / 700ms | ховеры / появление блоков / смена состояния |
| `--ease-out` | `cubic-bezier(0.16, 1, 0.3, 1)` | появление (expo-out) |
| `--ease-swap` | `cubic-bezier(0.65, 0, 0.35, 1)` | смены состояний, каретка |
| stagger | 40–60ms | каскад карточек |

Грамматика курсора (с crel.ch, сохраняем): при печати/стирании курсор горит непрерывно,
мигает только в паузе. `prefers-reduced-motion` — мгновенная замена без печати.

---

## 🗄 АРХИВ · Цвет v2 — цветовой слой (ревизия OnRamper)

> **Раздел в архиве.** Заменён «Слоем v3: необанкинг» (ниже). Хранится ради двух вещей:
> (1) измеренная система OnRamper по кадрам — она остаётся действительным референсом и
> используется в v3; (2) аргументация «один hue vs пара» — v3 её пересматривает, и сравнение
> двух решений нужно на созвоне. Токены `--grad-window-*`, `--wash-signal`, шкала теней и
> развилка CTA A/B **в работу не идут**.

Ревизия `refs-analysis.md`, где OnRamper прочли слишком минималистично. Уточнение заказчика:
отвергнут **только 3D** (hero и любые 3D-объекты), **не** цветность. Одобрено — сцены 2–17:
градиентные подложки карточек, цветные виджеты, скролл-микросценарии, градиентный финал.
Значения ниже измерены из стоп-кадров (`docs/refs/onramper-v2/`), динамика — из
`onramper-motion-notes.md`. Хексы OnRamper — референс energy, **не** копируем.

### OnRamper — измеренная система (референс)

| Роль | Измерено по кадрам | Конф. |
|---|---|---|
| CTA-заливка (вертикаль) | глубокий `#0F4CFF` → азур `#2A7BF6`, углы `#0051D7`/`#0043E8`; блик-диск cyan `#00F8FC` | ✅ кадр footer 3551px |
| Интерактив (кнопки-стрелки) | электрик-индиго `#361AFF`, ядро `#4E36FF` | ✅ footer |
| Подложки bento | royal `#0043E8` → азур `#0093EB`; индиго-виолет `#4E44EF` / `#1F00FF` | ⚠️ кадры 505px: hue верный, стопы приблизительно |
| Статус (чекмарк verified) | мятно-изумрудный, заливка `#5EFFC1` (ядро ~`#34E39B`) | ⚠️ |
| Спектральные подтёки на белых картах | синий `#1F00FF`, оранж `#FF7C00`, зелёный `#009B6B` — очень слабые подтёки из угла | ⚠️ |
| Рама / фон | белый `#FFFFFF`, холодная лаванда `#EFEEF3`; футер near-black `#151515` | ✅ |

**Вывод (подтверждает сводку notes):** OnRamper = двух-hue вольтаж (синий + индиго — **аналоговая**
пара, сливается в градиентах) + мятный статус-зелёный + белая рама + один полный градиентный финал.
Это не «цветной сайт», а белый сайт с цветными окнами и цветным финалом.

**Сверка драматургии цвета с кадрами (Шаг 1.3) — ПОДТВЕРЖДЕНА, с двумя уточнениями.**
Кадры показывают ровно «белая рама → цветные окна → цветной финал»: кадры 1–3 — светлая рама,
цвет только в подложках части карточек и внутри виджетов (кадр 1: залиты 2 карты из 4; кадр 3:
белые карты + слабые подтёки + синий интерактив); footer-кадр — единственная полная заливка;
затем чёрный футер. Уточнения к notes: (1) рама не чисто-белая, а **холодная лаванда** `#EFEEF3`;
(2) «пара» **аналоговая** (blue→indigo, соседние), не контрастная.

### Транспозиция в язык Crel

Не «OnRamper с нашим логотипом», а та же энергия на нашем корне: Space Grotesk, CLI-вольтаж,
швейцарские 90/10, острые углы. Ключевой сдвиг hue — у OnRamper вольтаж электрик-синий (их
бренд-код, `#1F00FF` не берём). У Crel вольтаж **уже выбран в коде** — терминальный зелёный
(фосфор CLI), настроен под AA (`app/globals.css`). Он и есть наш ответ синему OnRamper.

### Цветовые токены v2

Нейтрали — как в §1 (`--bg` `#FFFFFF` … `--ink` `#111111`; футер `--ink-900` `#0A0A0A`).

**Вольтаж** (в коде):

| Токен | Значение | Роль / контраст |
|---|---|---|
| `--accent` | `#00A344` | глифы, линии, интерактив на светлом — 3.3:1 (UI/крупное) |
| `--accent-deep` | `#067D3F` | мелкий текст статусов на светлом — 5.2:1 (AA) |
| `--accent-bright` | `#2BD576` | вольтаж на тёмном — 9.8:1 на `--ink` |

**Градиенты** (новое — глубина через тон ink + зелёный на глубоком крае, без второго hue):

| Токен | Угол · стопы | Применение |
|---|---|---|
| `--grad-window-ink` | 145° · `#151515 → #0B0B0C` | подложки «тёмных окон» (код-блок, часть bento) |
| `--grad-window-signal` | 145° · `#0A0A0A → #063A20 → #00A344` | «живая» карточка: ink теплеет в зелёный у активного края |
| `--grad-cta` | 135° · `#0A0A0A → #05341E → #00A344` | финальный CTA, вариант B; зелёный блик в дальнем углу |
| `--wash-signal` | radial · `rgba(0,163,68,.14) → transparent` | слабый одноцветный подтёк-настроение из угла белой карты (аналог спектральных bleeds OnRamper) |

Замечание по контрасту `--grad-cta`: белый текст на зелёном крае `#00A344` — лишь 3.3:1, поэтому
крупная типографика живёт на тёмных двух третях (белый на `#063A20` = 12.8:1), а зелёный уводится
в дальний угол — как cyan-диск OnRamper уходит от текста.

**Статус** (продуктовый цвет, только в мокапах): verified / live — `--accent-bright` на тёмном,
`--accent-deep` на светлом (чекмарки, `status: ok`, пульс-финал микросценария). Прочие статусы
мокапов (pending / error) — из продуктовой палитры QASIS, не из бренд-слоя, и живут только внутри мокапов.

**Логотипы** (правило из notes): партнёрские **ленты** (LogoLoop, лого под CTA) — всегда grayscale;
**отзывы / кейсы** — живые цветные лого (единственный «чужой» цвет, осознанно).

### Правила применения по ролям

| Роль | Цвет | Правило |
|---|---|---|
| Рама сайта (90%) | ахромат `--bg`/`--bg-alt`/`--ink` | вольтаж не попадает на фоны-плоскости |
| Подложки карточек | градиент **или** ахромат, чередование | не все карты цветные: ~1 из 3–4 в bento — градиентная (`--grad-window-*`), остальные плоские тон/1px |
| Интерактив | `--accent` / `--accent-bright` | курсор `_`, ссылки, активный таб/тумблер, кнопки-стрелки. Один вольтажный акцент в вьюпорте (принцип 1) |
| Статусы в мокапах | `--accent-deep`/`--accent-bright` | verified/live зелёным; финал микросценария — зелёный чекмарк с пульсом |
| Тёмный код-блок | `--grad-window-ink` + зелёная подсветка синтаксиса | контраст-якорь на светлой странице (приём OnRamper сц.9) |
| Финальный CTA | A: ахромат `--ink` / B: `--grad-cta` | развилка ниже, Вопрос 3 |

### Драматургия цвета по скроллу (правило)

Светлая рама (hero, ленты, текстовые секции — ахромат + зелёные искры интерактива) → цвет дозируется
в окнах (часть bento — градиентные подложки; виджеты-мокапы несут зелёный вольтаж и статусы; тёмный
код-блок как якорь) → **единственная полная заливка — финальный CTA** (вариант B) → чёрный футер.
Один полный градиент на страницу. Это транспонированная драматургия OnRamper: белая рама → цветные
окна → цветной финал, с синего вольтажа на зелёный и с мягкого 3D на острый терминал.

### Три вопроса ревизии — решения

**1. Один акцент или пара → один вольтажный hue в двух ролях (не пара брендов).**
OnRamper держит пару blue + indigo (декоративно сливаются в градиентах) плюс мятный статус.
Для Crel рекомендую **не** вводить второй бренд-hue: терминальный зелёный — единственный вольтаж,
играет и интерактив, и статус (в терминале зелёный = prompt/OK — это один сигнал). Ширину градиентам
даёт не второй hue, а тон ink с зелёным на глубоком крае (`--grad-window-signal`, `--grad-cta`).
Причина: второй бренд-цвет размывает «один CLI-акцент на экран» и делает нас похожими на OnRamper.
Кандидаты (финал): вольтаж `#00A344` / `#2BD576`; статус — тот же зелёный.
_Резерв, если на демо захотят два хроматических hue как у OnRamper:_ санкционированный второй — **не**
электрик-синий OnRamper, а холодный графит-индиго `#2A2A3A → #3D3D55` (десатурированный), работающий
как «холодная тень» за зелёным вольтажом, не конкурент бренд-цвету.

**2. Тени vs рамки 1px → системное правило по высоте, не по вкусу.**
- Плоские карты уровня страницы (bento-плитки, секции): разделение **тоном + хайрлайн 1px `--line`**, без тени.
- Градиентные подложки: разделяет **сама заливка** (цвет = граница, как у OnRamper); тень не нужна,
  опционально 1px внутренняя линия пониженной прозрачности для чёткости.
- Плавающие UI-мокапы: **единственная тень системы** `0 24px 64px rgba(17,17,17,.10)` (токен есть). Только они.
Итог: 1px/тон — для плоских и градиентных карт; тень — исключительно для парящих мокапов.
(OnRamper лифтит белые карты мягкой тенью на лаванде; мы лифтим тоном/линией, тень бережём — острее и более швейцарски.)

**3. Финальный CTA: чёрный или градиент → оба на демо (A/B).**
- **Вариант A — «Сдержанный».** CTA на `--ink` `#111`, белый Space Grotesk display, зелёный вольтаж
  в кнопке/курсоре (`get api keys_`). Ахроматичная кульминация; совпадает с текущим тёмным CTA.
  Драматургия: финал = максимальный контраст, не максимальный цвет.
- **Вариант B — «Заряд».** Полная заливка секции `--grad-cta` (135° · `#0A0A0A → #05341E → #00A344`),
  гигантский белый заголовок на тёмных двух третях, зелёный блик в дальнем углу. Единственный полный
  хроматический разлив страницы — кульминация драматургии цвета (наш ответ синему финалу OnRamper).
- Общее для A/B: белый display, зелёный курсор на CTA-команде, grayscale-лого партнёров под блоком.
Рекомендация к обсуждению: A — дефолт (сильнее держит швейцарский тон), B — «энергия OnRamper»
для цветного финала. Решаем на демо (связано с общим вопросом «сколько цвета»).

### Два слоя на демо: «Заряд» и «Сдержанный»

Цвет v2 (этот раздел) — слой **«Заряд»**: зелёный вольтаж, градиентные подложки, градиентный финал.
Прежний радикальный ахромат (`refs-analysis`: цвет только в UI-мокапах, ч/б плоскости, CTA чёрный)
сохраняем как слой **«Сдержанный»**: те же нейтрали и тот же зелёный вольтаж, но **без** градиентных
подложек и с CTA-вариантом A. Оба слоя — на одном каркасе и одной палитре: переключение косметическое
(включить/выключить `--grad-*` и `--wash-signal`). Показываем заказчику оба на демо — это и есть
развилка «сколько цвета», согласованная с CTA A/B.

---

# Слой v3: необанкинг **[рабочий]**

Смена визуального курса. Прежние слои читали OnRamper как «белый сайт с цветными окнами» и
держали швейцарскую сухость: ахромат, острые углы, хайрлайны, цвет по каплям. Текущее состояние
(`docs/review/v2/`) этот замысел исполняет корректно — и именно поэтому видно, чего не хватает:
сайт **честный, но не дорогой**. Плоскости белые и серые, карточки различаются только тоном,
единственный цветной объект на весь скролл — чёрная плитка с зелёным подтёком.

v3 меняет не структуру, а материал. Ориентир — язык современного необанкинга: матовое стекло,
софт-градиенты, свечение, насыщенные заливки. Референс подачи — bento OnRamper под hero
(`docs/refs/onramper-v2/onramper 1.png`): **чередование** светлых карточек с визуалом и карточек
с уверенной градиентной заливкой. Ключевое слово — уверенной: у OnRamper залитая карта берёт
полный royal-blue от края до края, а не подтёк из угла. Наш аналог обязан быть таким же
решительным на зелёном.

Пластический словарь дополнительно сверен с `docs/wembi-motion-notes.md` (папки `docs/refs/wembi/`
в репозитории нет — использованы посценные заметки): frosted-объёмы, editorial-масштаб заголовков,
приём «ассет обрезан краем кадра», двухскоростная моторика.

**Что v3 НЕ трогает:** структуру страниц, контент, тумблер и хореографию «Перепечатка» (§3),
типографику и сетку (§1), запрет фигуративного 3D.

---

## v3.1. Цвет

### Рекомендация: зелёный остаётся единственным брендовым hue и получает градиентного партнёра

Формулировка точная, потому что вопрос из двух частей.

**Бренд-hue — один: вольтаж-зелёный.** Он в логотипе-интонации, в курсоре, в статусах, в коде.
Второй *брендовый* цвет размывает узнаваемость и подтаскивает нас к OnRamper, чей код —
двухцветный синий.

**Но односоставный градиент не бывает дорогим.** Заливка «зелёный → тёмно-зелёный» — это ramp
по светлоте, глаз читает её как «одна краска с тенью», а не как свет. Именно поэтому текущая
плитка «Widget and White Label API» выглядит как чёрный прямоугольник с зеленцой, а не как
объект. У OnRamper богатство даёт **аналоговая пара** blue→indigo (соседние hue, сливаются без
грязи). Наш эквивалент — движение по зелёному в сторону **циан-бирюзы**: 145° → 176°, тот же
интервал, что у OnRamper, тот же принцип. Циан при этом **не бренд-цвет**: он не появляется
самостоятельно — только как дальний стоп градиента, цвет свечения и rim-light на ассетах.

Плюс глубокий якорь — **pine**, почти чёрный зелёный. Он даёт градиентам ход по светлоте
(без него любая заливка липнет к «аптечному» среднему зелёному) и служит тёмной плоскостью
вместо нейтрального `#111`.

Итог — семья из одного hue-ядра с двумя краями: **pine → вольтаж → jade → teal → cyan-glow.**
Почему не лайм/шартрез в тёплую сторону: он читается как крипто-деген и не тянет премиальность.
Почему не синий: чужой бренд-код (зафиксировано ранее, решение в силе).

### Токены v3 — нейтрали и плоскости

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#FFFFFF` | базовая плоскость |
| `--bg-alt` | `#F4F7F5` | чередование секций — нейтраль с холодным зелёным подтоном (0.5% хромы) |
| `--bg-mist` | `#EEF3F0` | второй шаг чередования, подложки светлых карточек |
| `--bg-bloom` | `--bg` + ambient-радиал (см. v3.3) | «атмосферная» секция — замена разделителя |
| `--surface` | `#FFFFFF` | подложки UI-мокапов (всегда чисто-белые: мокап = продукт) |
| `--ink` | `#0B1210` | текст и заголовки — не нейтральный чёрный, а зелёно-чёрный |
| `--ink-forest` | `#052318` | текст **на светлых цветных заливках** (см. правило текста) |
| `--ink-soft` | `#5C6A64` | вторичный текст |
| `--ink-invert` | `#FFFFFF` | текст на тёмном и на насыщенных заливках |

Подтон обязателен: ахроматичные серые рядом с насыщенным зелёным читаются как грязь. Хрома
минимальная — плоскости остаются спокойными, но принадлежат одной семье.

### Токены v3 — зелёная семья

| Токен | Значение | Роль |
|---|---|---|
| `--pine-900` | `#04150E` | глубочайший якорь: футер, тёмные окна |
| `--pine-800` | `#04291B` | тёмный край градиентов (белый текст 15.7:1) |
| `--pine-600` | `#067D3F` | = прежний `--accent-deep`; мелкий текст на светлом (5.2:1 AA) |
| `--accent` | `#00A344` | **ядро идентичности, без изменений**: курсор, интерактив, линии |
| `--jade` | `#00B85C` | средний стоп заливок, hover-состояния |
| `--accent-bright` | `#2BD576` | вольтаж на тёмном (9.8:1 на `--ink`), статусы live |
| `--teal` | `#00C9A7` | переходный стоп градиентов |
| `--cyan-glow` | `#22E3D8` | дальний стоп, свечение, rim-light ассетов |

### Три градиента заливок

Три, а не два — потому что bento требует различимости соседних залитых карточек. Различаются
не hue, а **температура и светлота**: тёмная, средняя, светящаяся.

| Токен | Стопы | Характер | Текст |
|---|---|---|---|
| `--grad-abyss` | 145° · `#04150E 0%` → `#04291B 55%` → `#00693A 100%`, + радиал `--cyan-glow` 22% в дальнем углу | тёмная плитка, контраст-якорь; наследник тёмного код-блока | белый |
| `--grad-signal` | 145° · `#067D3F 0%` → `#00B85C 52%` → `#00C9A7 100%` | **та самая уверенная заливка** — полный цвет от края до края | белый **только в верхней трети** (на `#067D3F` = 5.2:1); ниже — `--ink-forest` |
| `--grad-halo` | 160° · `#2BD576 0%` → `#00C9A7 48%` → `#22E3D8 100%` | светящаяся карта, самый яркий объект страницы | `--ink-forest` (8.6–10.3:1) |

**Правило текста на заливке (жёсткое, снимает все споры о контрасте):**
светлее `--accent` `#00A344` → текст `--ink-forest`; темнее `--pine-600` `#067D3F` → текст белый.
Сам `#00A344` — граница: белый на нём 3.3:1 (только display 40px+), `--ink-forest` 5.0:1 (годен везде).
Практический вывод: **на ярких картах текст тёмный.** Это и решает AA, и выглядит дороже
белого — приём светлых финтех-карточек, а не крипто-баннера.

**Правило построения градиента:** ход по светлоте ≥ 45% и обязательный проход либо через
`--pine-800` (тёмная ветка), либо через `--teal`/`--cyan-glow` (светящаяся ветка). Градиент из
одних средних зелёных запрещён — именно он даёт «аптеку».

### Драматургия цвета по скроллу (переписано)

Прежнее правило «один полный градиент на страницу» **отменено** — оно и делало сайт бледным.

Новое: **шахматка.** Цвет распределён по всему скроллу равномерными вспышками, а не копится к
финалу. Порядок плоскостей: светлый hero → bento с чередованием (см. v3.6) → светлая текстовая
секция → `--bg-bloom` → bento/табы с чередованием → CTA `--grad-signal` во всю ширину →
футер `--pine-900`.

Норма насыщенности: **в любом вьюпорте виден минимум один цветной объект и максимум два**
цветных объекта уровня секции. Ноль — провал (сегодняшняя проблема), три — ярмарка.

---

## v3.2. Стекло (frosted glass)

Три рецепта. Больше не заводим — стекло дорого и по рендеру, и по вниманию.

| Токен | Значение | Где |
|---|---|---|
| `--glass-light` | `rgb(255 255 255 / .72)` · `blur(24px) saturate(180%)` · border `1px rgb(255 255 255 / .8)` + `inset 0 1px 0 rgb(255 255 255 / .9)` | **шапка** (sticky), светлые оверлеи |
| `--glass-tint` | `rgb(255 255 255 / .14)` · `blur(20px) saturate(160%)` · border `1px rgb(255 255 255 / .26)` + `inset 0 1px 0 rgb(255 255 255 / .38)` | панели **поверх заливок**: чипы-статусы, бейджи, слоистые стопки на градиентных картах |
| `--glass-dark` | `rgb(4 21 14 / .58)` · `blur(28px) saturate(160%)` · border `1px rgb(255 255 255 / .1)` + `inset 0 1px 0 rgb(255 255 255 / .14)` | оверлеи на тёмных плитках, dropdown меню на `--grad-abyss` |

Шкала блюра: `--blur-s: 12px` (чипы) · `--blur-m: 24px` (шапка, панели) · `--blur-l: 40px`
(полноэкранные оверлеи, мобильное меню).

**Внутреннее свечение** — обязательная часть рецепта, не украшение: `inset 0 1px 0` по верхней
кромке даёт стеклу толщину. Без него полупрозрачный слой читается как «забыли залить фон».

### Правила стекла

1. **Шапка — главный носитель стекла.** `--glass-light`, высота 64px, sticky, контент под ней
   просвечивает и размывается. Приём OnRamper: шапка не отделяется линией, она отделяется
   средой. Логотип, тумблер-ось и pill-CTA лежат прямо на стекле.
2. **Максимум 3 backdrop-filter элемента в вьюпорте** (шапка + два). `backdrop-filter` — дорогая
   операция; больше трёх на средних ноутбуках даёт просадку скролла.
3. **Никогда не анимировать радиус блюра.** Анимируем только `opacity` и `transform` стекла.
4. **Фолбэк обязателен:** `@supports not (backdrop-filter: blur(1px))` → непрозрачная заливка
   (`#FFFFFF` `.94` для light, `#082016` `.96` для dark). Текст на шапке не имеет права терять
   контраст ни в одном браузере.
5. **Стекло только там, где под ним есть что размывать.** Стеклянная карточка на плоском белом
   фоне — пустой эффект; такая карточка должна быть просто светлой (`--bg-mist`).
6. **Текст на стекле — только `--ink` или белый, никогда `--ink-soft`.** Полупрозрачная подложка
   съедает контраст вторичного текста.

---

## v3.3. Свечение (glow)

Свечение — это **цветная тень**, а не ореол вокруг текста. Отсюда вся система.

| Токен | Значение | Применение |
|---|---|---|
| `--glow-s` | `0 0 20px rgb(0 163 68 / .35)` | точка статуса live, курсор `_` на тёмном, активный таб |
| `--glow-m` | `0 12px 40px rgb(0 163 68 / .28)` | hover pill-кнопки, парящий мокап над цветной картой |
| `--glow-l` | `0 32px 120px rgb(0 163 68 / .22)` | подъём градиентной карты над плоскостью секции |
| `--glow-ambient` | радиал `800px` · `rgb(0 201 167 / .10) → transparent 70%` | атмосферный блик за секцией (`--bg-bloom`, hero) |
| `--glow-cyan` | те же радиусы, `rgb(34 227 216 / α)` | свечение **на тёмном/цветном**: α до `.45` |

### Правила свечения

1. **Интенсивность зависит от подложки.** На белом зелёный glow выше `.16` alpha читается как
   грязное пятно — на светлых плоскостях `α ≤ .16`. На тёмном и на заливках — до `.45`.
2. **Максимум два источника свечения в вьюпорте.** Плюс один ambient-радиал фона (он не считается —
   он среда, а не объект).
3. **Glow не наносится на текст.** Единственное исключение — underscore-курсор и точка статуса
   live: объекты размером ≤ 16px, где свечение = «горит», а не «размыто».
4. **Glow заменяет тень.** Нейтральная тень `rgba(17,17,17,…)` из v2 отменяется полностью:
   тень парящих мокапов теперь `--glow-l` (цветная, зелёная) — объект не «лежит на бумаге»,
   а «светится над плоскостью». Это самый быстрый способ добрать «дорого».
5. **Ambient-блики дрейфуют медленно или не дрейфуют.** Если анимируем — `--d-ambient: 16s`,
   амплитуда ≤ 4% вьюпорта, `transform` only, полностью выключается при `prefers-reduced-motion`.
   Это «медленная» скорость двухскоростной системы (wembi сц. 9); интерфейс остаётся snappy.

---

## v3.4. Пластика (радиусы, кнопки)

Острая шкала 2/4/8/12 **отменена**. Идентичность не теряется: она держится не углом, а
типографикой (Space Grotesk, двоеточие-ось, курсор) — то есть слоем, который v3 не трогает.

| Токен | Было (v2) | **Стало (v3)** | Применение |
|---|---|---|---|
| `--r-s` | 2px | **10px** | чипы-статусы, мелкие бейджи, поля внутри мокапов |
| `--r-m` | 4px | **16px** | поля ввода, ячейки списков, вложенные блоки |
| `--r-l` | 8px | **24px** | карточки |
| `--r-xl` | 12px | **32px** | крупные bento-плитки, рамы UI-мокапов |
| `--r-2xl` | — | **40px** | полноширинные панели, секция финального CTA |
| `--r-pill` | — | **999px** | **все кнопки**, чипы, тумблер, табы |

**Кнопки — овальные (pill), без исключений.** Первичная: заливка `--ink` или `--grad-signal`,
текст по правилу контраста, высота 48px, паддинг 28px, hover — `--glow-m` + `translateY(-1px)`.
Вторичная: `--glass-light` с бордером, тот же радиус. Иконочные — круг 44px (минимум touch-target).

**Правило вложенности:** радиус ребёнка = радиус родителя − внутренний отступ, минимум `--r-s`.
Мокап внутри плитки `--r-xl` (32px) с паддингом 16px получает 16px — иначе острая рама внутри
круглой выдаёт «прилепили».

**Правило одного радиуса на объект:** не смешиваем скруглённые и острые углы в одном элементе.
Обрезка ассета краем карточки идёт по скруглённому контуру (`overflow: hidden` на плитке).

---

## v3.5. Разделители — сняты как класс

`1px --line` больше не существует как элемент дизайн-системы. Сегодня хайрлайны несут три разные
работы, и каждая получает свою замену.

| Что разделяла линия | Замена v3 | Правило |
|---|---|---|
| Секцию от секции | **Воздух + тон** | вертикальный ритм 112/160 (без изменений) + смена плоскости `--bg` / `--bg-alt` / `--bg-mist` / `--bg-bloom`. Если тон не меняется — интервал растёт до 160px |
| Секцию, где тон не меняется | **Атмосфера** | плоскость `--bg-bloom`: тот же белый + `--glow-ambient` в углу. Смена среды вместо реза. Это же — «ритмический вдох» вместо декоративной паузы wembi |
| Карточку от карточки | **Сама карточка** | у карточки есть заливка (`--bg-mist` / градиент) и радиус ≥ 24px — граница уже нарисована. Гэп 24px |
| Строку списка от строки (секция approach, таблицы фич) | **Ячейка** | строка становится блоком `--r-m` на `--bg-mist` (на светлом) или `--glass-tint` (на заливке), гэп 8px. Ноль линий |

**Единственное разрешённое место для 1px-линий — внутри UI-мокапов.** Там линия — часть
изображаемого продукта, а не нашего языка. Граница проходит по раме мокапа.

Побочный эффект, который надо принять: без линий секция без цветного объекта выглядит пусто.
Отсюда норма насыщенности из v3.1 (минимум один цветной объект в вьюпорте) — она не декоративная,
она структурная.

---

## v3.6. Bento: чередование (правило подачи)

Прямой перенос приёма OnRamper с нашей палитрой.

- **Ритм заливок: примерно каждая вторая плитка цветная**, не «одна из четырёх» как в v2.
  В сетке 2×2 — две цветные по диагонали. В 3+3 — не более трёх подряд одного типа.
- **Светлая плитка** = `--bg-mist`, `--r-xl`, заголовок `--ink`, снизу генеративный ассет или
  UI-мокап, **обрезанный нижним/правым краем плитки** (приём OnRamper + wembi сц. 2).
- **Цветная плитка** = `--grad-signal` / `--grad-halo` / `--grad-abyss` во всю площадь, заголовок
  по правилу контраста, визуал — ассет с прозрачным светом поверх заливки или стеклянные панели
  `--glass-tint`.
- **Соседние цветные плитки берут разные градиенты.** Две `--grad-signal` рядом = одно большое
  пятно, плитки склеиваются.
- **Тёмная `--grad-abyss` — одна на секцию**, играет роль якоря (её место сейчас занимает
  плитка «Widget and White Label API» — она остаётся тёмной, но перестраивается по v3).
- Размерная вариативность обязательна: плитка-герой 2×1 или 2×2 на секцию, остальные 1×1.
  Ровная сетка одинаковых карточек — то, из-за чего текущая Platform читается как таблица.

---

## v3.7. CLI-акценты в ярком языке

Ревизия по принципу: **CLI остаётся синтаксисом, перестаёт быть костюмом.** В ахроматичном слое
терминальность держала всю индивидуальность и потому лезла в форму каждого элемента. В ярком
слое индивидуальность несёт цвет и материал — CLI можно сжать до знаков и выиграть в дороговизне.

**Остаётся (ядро, не обсуждается):**

| Приём | Статус в v3 |
|---|---|
| Двоеточие-ось: `c:rel_`, тумблер `services : platform`, hero-команда | без изменений — это бренд |
| Лейблы глав `01: capabilities` | остаются: строчные, трекинг +10%, цвет `--pine-600` на светлом / `--accent-bright` на тёмном. Форма — просто текст, **без рамки и без чипа** |
| Underscore-курсор | остаётся, усиливается: `--glow-s` на тёмных и цветных подложках. Один мигающий курсор в вьюпорте |
| «Перепечатка» и весь motion-словарь печати | без изменений (§3.4) |
| Тёмное окно с кодом | остаётся как приём, но это теперь `--grad-abyss` + `--r-xl` + подсветка синтаксиса `--accent-bright` / `--cyan-glow`, а не плоский чёрный слэб |

**Уходит:**

| Приём | Почему | Замена |
|---|---|---|
| Статусы как голая строка `status: mica_casp — in progress` | в ярком слое читается как отладочный вывод, а не как достижение | стеклянный pill `--glass-tint` / `--bg-mist`, точка `--accent` с `--glow-s`, текст «mica casp · in progress». Синтаксис двоеточия сохраняется в тексте пилюли |
| Табличные ряды с хайрлайнами (secции approach, licensing) | это и есть разделители, снятые в v3.5 | ячейки `--r-m` |
| ASCII-обвязка `[ … ]` вокруг тумблера и кнопок | скобки + pill-радиус = визуальный конфликт | тумблер — стеклянный pill-сегментед с двоеточием-осью внутри |
| Терминальная зелёная моноширинная типографика вне мокапов | конкурирует с Space Grotesk, читается дёшево | mono только внутри код-блока |
| Правило «один CLI-акцент на экран» как ограничитель цвета | подменяло дозировку цвета дозировкой терминальности | заменено нормой насыщенности v3.1 |

Пропорция меняется с 90/10 на **80/20 в пользу цвета и материала**: 80% экрана — воздух,
типографика, светлые плоскости; 20% — цветные заливки, стекло, свечение. CLI живёт внутри
этих 80% как знаки, а не как фактура.

---

## v3.8. Дельты к принципам §2

| Принцип §2 | Статус в v3 |
|---|---|
| 1. Швейцарская база 90/10 | **изменён** → 80/20, см. v3.7 |
| 2. Двоеточие — структурный знак | без изменений |
| 3. Underscore-курсор | без изменений (+ glow) |
| 4. Иерархия размером | без изменений; заголовки глав можно укрупнить ×1.5 (editorial-масштаб, wembi сц. 2) |
| 5. Продукт — фрагментом интерфейса | без изменений; подложка мокапа `--surface` = чисто-белая, тень → `--glow-l` |
| 6. Плоскости тоном, углы острые | **отменён и переписан**: см. v3.4 (радиусы), v3.5 (разделители), v3.1 (цвет) |
| 7. Всё движение — из печати | **дополнен**: двухскоростная система — медленный ambient-дрейф свечения (16s) + snappy интерфейс (200/400ms). Запреты (bounce, parallax, 3D-повороты, глитч, scanlines) в силе |
| 8. Приёмы OnRamper на нашем корне | **усилен**: чередование заливок теперь ~50/50, а не 1 из 4 |

### Анти-паттерны v3 (обновление списка §2)

**Снято** (было запретом в v2, теперь разрешено или обязательно): мягкие радиусы; полноцветные
заливки чаще одного раза на страницу; стекло и blur; свечение; второй hue **в роли стопа
градиента**.

**В силе:** фигуративное 3D — сферы, глобусы, монеты, устройства, руки (запрет заказчика);
электрик-синий `#1F00FF` и лиловая рама `#EFEEF3` OnRamper; motion-запреты принципа 7.

**Новое в v3:**
- Радужные / мультихромные градиенты (розовый→фиолетовый→оранжевый) — мгновенно «AI-дефолт».
- Средне-зелёная плоская заливка без хода по светлоте — «аптека».
- Стекло на плоском фоне, где нечего размывать.
- Свечение под текстовым блоком (нечитаемо) и glow-обводка вокруг заголовков.
- Больше двух цветных объектов уровня секции в одном вьюпорте.
- Мягкие круглые «капсульные» иллюстрации — генеративные ассеты остаются штриховыми/пластинчатыми
  (подтверждено wembi сц. 4: их частицы — цилиндры-штрихи, не таблетки).

---

## v3.9. Порядок миграции кода

Токены в `app/globals.css` — из v2. Порядок, при котором сайт ни на одном шаге не выглядит
недоделанным (важно: радиусы и разделители нельзя менять по отдельности):

1. **Палитра + градиенты + glow** в `@theme`; нейтрали получают зелёный подтон. Визуально пока
   почти ничего не меняется — фундамент.
2. **Пластика одним коммитом:** радиусы + pill-кнопки + снятие `--color-line` вместе со всеми
   его употреблениями (ячейки вместо строк с линиями). Раздельно — получим круглые карточки
   с линиями внутри.
3. **Шапка на стекло** + `@supports`-фолбэк.
4. **Bento-чередование**: раскраска плиток Platform и Services по v3.6, тень мокапов → `--glow-l`.
5. **Ambient-свечение** и `--bg-bloom` вместо мест, где раньше стояли разделители.
6. **CLI-ревизия** (v3.7): статус-пилюли, тумблер-сегментед, код-блок на `--grad-abyss`.
7. Генеративные ассеты — по мере готовности, слоты закладываются на шаге 4.

Проверка после каждого шага: 375 / 768 / 1280 / 1440, светлый и тёмный системный режим, контраст
текста на всех заливках, `prefers-reduced-motion`, клавиатурный фокус (обводка `--accent` на
светлом / `--accent-bright` на тёмном — правило v2 сохраняется).

---

# Бриф на визуалы (генерация — отдельно)

Это техзадание на генерацию, не на вёрстку. Стиль един для всех 15 ассетов; различаются только
сюжеты. Генерация выполняется отдельно — здесь только бриф.

## Общий стиль

**Материал.** Толстое оптическое стекло: глянцевая поверхность, матовое «замороженное» ядро
(frosted core), чистый скол по торцу. Рефракция мягкая, без хроматических аберраций и без
радужных бликов. Поверхности — плоскости, ленты, пластины, кольца, штрихи. Никакой органики,
никакого металла, никакого пластика.

**Свет.** Одна мягкая ключевая сверху-слева (high-key), холодный rim-light `--cyan-glow` справа
по кромке, плюс собственное объёмное свечение объекта. Тени цветные (зелёные/бирюзовые), мягкие,
длинные — никогда серые.

**Палитра — замок.** Только семья v3: `#04150E` · `#04291B` · `#067D3F` · `#00A344` · `#00B85C` ·
`#2BD576` · `#00C9A7` · `#22E3D8` + белый и дымчато-нейтральный. Любой синий, фиолетовый,
оранжевый, розовый — брак.

**Фон.** Два варианта в зависимости от типа плитки: (a) для светлых карточек — near-white
`#F7F9F8` с мягкой цветной тенью объекта; (b) для градиентных карточек — глубокий `#04291B`,
объект светится сам. Фон — часть ассета, прозрачность не требуется.

**Камера и композиция.** Фронтально или лёгкая изометрия, всегда статичная. Объект **обрезан
краем кадра** (приём wembi сц. 2 / OnRamper) — не по центру, не целиком. Композиция читается
за 0.5 секунды: один жест, одна идея.

**Формат.** approach — 3:4 вертикальные; bento Platform и services — 4:3; экспорт 2×, WebP;
путь `public/assets/<серия>/<slug>.webp`.

**Стоп-лист.** Фигуративные объекты (сферы, монеты, глобусы, телефоны, устройства, руки, люди);
текст и элементы UI внутри ассета; hero-масштаб; пастельная радуга; bloom до потери формы;
капсульные/таблеточные формы; логотипы.

> ⚠️ **Требует слова заказчика в четверг.** Формально «глянцевое стекло с объёмным светом» — это
> 3D-рендер, а 3D в §2 запрещён. Читаю запрет как «нет фигуративным 3D-объектам и hero-3D»
> (отвергались глобусы, монеты, сферы), а не как запрет на материал и свет. Граница в брифе
> проведена явно: абстрактные плоскости и потоки — можно, узнаваемые предметы — нельзя. Если
> заказчик читает запрет шире, откат — те же сюжеты в плоской векторной подаче с градиентными
> заливками и без объёмного света; стиль-блок переписывается, сюжеты остаются.

## Серия 1 — approach (переработка, 3 шт, 3:4)

| # | slug | Сюжет |
|---|---|---|
| 1.1 | `scan` | Плотный веер тонких стеклянных штрихов проходит сквозь узкую световую щель; на выходе часть штрихов подсвечена цианом — прочитано и размечено. |
| 1.2 | `rails` | Три параллельных стеклянных жёлоба уходят в глубину, поток частиц свободно перетекает между ними по боковым перемычкам — ни один не единственный. |
| 1.3 | `gates` | Поток частиц идёт сквозь череду стеклянных колец-гейтов, каждое горит своим ободом, последнее — сплошным изумрудным свечением. |

_Замена прежней серии: «таблетки» → штрихи-цилиндры (wembi сц. 4), плоские силуэты → стекло._

## Серия 2 — bento Platform (6 шт, 4:3)

| # | slug | Сюжет |
|---|---|---|
| 2.1 | `qasis` | Стопка полупрозрачных стеклянных пластин-документов, по каждой сверху вниз идёт световой сканирующий луч; верхняя уже обведена изумрудным ободом. |
| 2.2 | `ramp` | Две встречные стеклянные ленты-потока — холодная циановая и изумрудная — скрещиваются в светящемся узле по центру кадра. |
| 2.3 | `accounts` | Решётка мелких стеклянных ячеек, часть раскрылась в отдельные светящиеся капсулы-счета и веером уходит вправо за край. |
| 2.4 | `cards` | Три стеклянные карты-пластины парят уступом, торцы ловят цианово-изумрудный рефлекс, под ними мягкие цветные тени. |
| 2.5 | `terminal` | Стеклянная плита-терминал под углом, из неё вверх бьёт узкий конус света, внутри конуса висят частицы-транзакции. |
| 2.6 | `widget` | Стеклянный прямоугольник-окно вставлен заподлицо в чужую матовую поверхность, шов по периметру светится изумрудной нитью. |

## Серия 3 — services (6 шт, 4:3)

| # | slug | Сюжет |
|---|---|---|
| 3.1 | `implementation` | Разрозненные стеклянные модули сходятся и стыкуются в единую плиту, стыки светятся изнутри. |
| 3.2 | `architecture` | Чертёжная сетка из светящихся линий поднимается из плоскости в объём и обрастает стеклянными слоями-этажами. |
| 3.3 | `licensing` | Вертикальная стопка стеклянных дисков-печатей: нижние тусклые, верхняя залита сплошным изумрудным свечением. |
| 3.4 | `vendors` | Ряд одинаковых стеклянных пластин, одна выдвинута вперёд и подсвечена цианом, остальные уходят в матовую расфокусировку. |
| 3.5 | `mobile` | Стеклянная вертикальная пластина-экран, по которой снизу вверх наливается градиентная заливка, края ловят свет. |
| 3.6 | `support` | Замкнутый стеклянный контур-кольцо, по которому непрерывно бежит одиночный изумрудный импульс. |

**Сквозная проверка серии:** три ассета одной серии, поставленные рядом, должны различаться
**сюжетом**, а не светом и палитрой (wembi сц. 5). Если разница читается только как «тут
светлее» — перегенерировать.

---

## 2. Принципы дизайн-языка

> Список ниже — редакция v2. Действующая редакция = этот список **плюс дельты из «v3.8»**
> (принципы 1, 6, 7 изменены, остальные в силе). При расхождении выигрывает v3.

1. **Швейцарская база, терминальная интонация — 90/10.** Девяносто процентов экрана — чистый
   гротеск, воздух, хайрлайны, ахромат. CLI-слой — приправа, не блюдо. Один CLI-акцент на экран.
2. **Двоеточие — структурный знак системы.** Лейблы секций (`01: platform`), статусные подписи
   (`status: mica_casp — in progress`), нумерация с ведущим нулём и ось переключателя.
   В H1/H2 контентных секций и в body двоеточие как приём не используется.
3. **Underscore-курсор — знак живого состояния.** Hero-логотип, активное слово тумблера,
   hover ключевых CTA (`get started_`). Не более одного мигающего курсора в вьюпорте.
4. **Иерархия — размером.** Два начертания, контраст масштабов (112px против 16px),
   никаких цветных заголовков и жирного в body.
5. **Продукт показываем фрагментом настоящего интерфейса.** Правило OnRamper: не скриншот
   целиком, а увеличенный фрагмент с реальными данными (числа, валюты, статусы KYC) на светлой
   подложке `--surface`, обрезанный краем карточки. Тень разрешена только мокапам.
6. **Плоскости — тоном и хайрлайном, углы — острые, глубина — дозированный цвет.** Секции
   чередуют ахроматичный фон; разделение — тон + 1px `--line` (правило теней/рамок — «Цвет v2 →
   Вопрос 2»). Радиусы острые 2–12px (мягкость OnRamper 24px не берём). **Ревизия:** градиент и
   glass-панели теперь РАЗРЕШЕНЫ как дозированный слой глубины — подложки части карточек, slide-in
   glass-стопки, финальный CTA (см. «Цвет v2»), но только на нашем зелёном вольтаже и в острой
   геометрии. Единственный визуальный запрет — 3D (см. анти-паттерны).
7. **Всё движение — из печати.** Словарь анимаций: type / erase / blink / caret-sweep /
   stagger-появление. Разрешён **скролл-микросценарий**: мокап разыгрывает процесс (выбор →
   подтверждение → зелёный чекмарк с пульсом) на scroll-reveal — усиление принципа 5, без новых
   сущностей. Запрещены: bounce, parallax, 3D-повороты, глитч, scanlines. Терминальность —
   интонация паузы и курсора, не костюм хакера.
8. **Приёмы OnRamper на нашем корне (сцены 2–17, «Цвет v2»).** Одобренная часть OnRamper,
   адаптированная под Crel:
   - **Градиентные подложки части bento** — не всех карт: чередование градиентная / плоская (~1 из 3–4).
   - **Скролл-микросценарии в мокапах** — мокап играет процесс, а не статичная картинка (принцип 7).
   - **Slide-in glass-стопки** — полупрозрачные панели въезжают каскадом со stagger на reveal (глубина
     без 3D); «выглядывающие» из-за края панели как декор перехода секций.
   - **Тёмный код-блок как контраст-якорь** на светлой странице, зелёная подсветка синтаксиса.
   - **Правило логотипов** — ленты grayscale, отзывы / кейсы живые цветные.

### Анти-паттерны (не брать) 🗄 **[редакция v2 — действующий список в «Анти-паттерны v3»]**

- **3D в любом виде** — hero-3D, 3D-глобусы и сферы, стеклянные монеты, глянцевые волны, летающие
  объекты (отвергнуто заказчиком; замена — живые мокапы-фрагменты). Это **единственный** визуальный
  запрет из OnRamper — цветность, градиенты и glass-панели одобрены.
- Мягкие 24px-радиусы OnRamper (у нас острая шкала 2–12px).
- Лиловая рама `#EFEEF3` и электрик-синий `#1F00FF` OnRamper — чужой бренд-код.
- Второй хроматический бренд-hue (кроме зелёного вольтажа) — держим в резерве, по умолчанию не вводим.
- Motion: bounce, parallax, 3D-повороты, глитч, scanlines (принцип 7).

---

## 3. Концепция переключателя Services / Platform

Структурное требование заказчика: два равнозначных состояния лендинга, тумблер в духе
Transak Business/Individual, но переключение — фирменный приём Crel.

### 3.1. Ключевая идея: двоеточие как ось **[решено]**

`c:rel` читается как `key:value` — двоеточие уже делит бренд на «ключ» и «значение».
Переносим этот синтаксис на переключатель: двоеточие становится **неподвижной осью**, вокруг
которой меняется значение. Симметрия относительно двоеточия — визуальное доказательство
равнозначности направлений: форма тумблера буквально выражает главный бизнес-тезис брифа.

Синтаксическая система целиком:

- логотип: `c:rel_` — ключ бренда;
- тумблер: `services : platform` — ось между равными значениями;
- hero: `c:services_` / `c:platform_` — команда с аргументом (эхо состояния);
- лейблы секций: `02: licensing` — тот же знак на нижних уровнях.

### 3.2. Варианты исполнения тумблера

**Вариант A — «ось» в шапке** ⭐ рекомендуемый

Позиция Transak (сразу справа от логотипа, до пунктов меню — читается как «выбор мира»),
исполнение своё. Прямоугольник с обводкой `1px --line`, радиус 4px, внутри два строчных слова
и двоеточие-ось по центру:

```
[ services : platform ]      активное — --ink + курсор:  [ services⎯: platform ]  →  [ services : platform_ ]
```

- Активное слово: `--ink`, weight 500, с underscore-курсором после слова (горит статично,
  мигает только у активного состояния при бездействии страницы — опционально).
- Неактивное: `--ink-soft`, weight 400. Двоеточие всегда `--ink` — оно принадлежит системе.
- При переключении слова не двигаются: вес и цвет перетекают (200ms), курсор «перепрыгивает»
  к новому слову. Никакой скользящей пилюли — Transak-паттерн узнают именно по ней, у нас её нет.
- Шапка sticky → тумблер доступен всегда.

Плюсы: постоянная доступность; двоеточие-ось = равнозначность; ноль сходства с капсулой
Transak; тривиален на мобиле (те же два слова).
Риски: аффорданс — «это кликабельно?» Митигируется обводкой-рамкой, hover-состоянием
(курсор `_` подставляется к слову под мышью) и тем, что hero дублирует состояние крупно.

**Вариант B — «команда» в hero**

Hero-заголовок — гигантский `c:services_` / `c:platform_` (112px, TextType). Под ним или
рядом — пара слов-переключателей; клик перепечатывает аргумент команды прямо в заголовке.
Тумблер и заголовок — один объект.

Плюсы: максимально фирменно, вау-момент — сама точка входа; идеальная сцена для морфа.
Риски: недоступен после скролла (нужен дубль в шапке → два контрола, что и приводит к
гибриду с A); заголовок-неологизм требует смыслового подзаголовка-расшифровки
(«Swiss digital asset consulting» / «The digital asset rail for financial apps»);
на мобиле 112px-слово `platform` на грани.

**Вариант C — плавающий док**

Капсула fixed внизу по центру (или под шапкой), всегда в вьюпорте.

Плюсы: максимальная доступность, не конфликтует с логотипом.
Риски: app-интонация чужда швейцарской статике сайта; перекрывает контент на мобиле;
конкурирует с CTA за нижнюю зону; выглядит «виджетом», а не частью системы. **Отклонить.**

**Рекомендация: A + hero-эхо из B.** Канонический контрол — ось в шапке (всегда доступен).
Hero крупно показывает состояние командой `c:services_` и перепечатывается при переключении,
но кликабельный контрол — один, в шапке (клик по hero-команде — приятный бонус, не основной
путь). Это снимает риск двух конкурирующих контролов и сохраняет вау.

### 3.3. Анатомия переключения **[решено]**

Принцип: **общий каркас — сменная начинка**. Оба состояния — полноценные лендинги равной
глубины (5–6 секций), ни одно не является «главным».

| Слой | Общее (persistent) | Сменное (per-state) |
|---|---|---|
| Шапка | логотип `c:rel_`, тумблер, каркас меню, CTA-кнопка | якоря меню (ведут в секции своего состояния), текст CTA |
| Hero | композиция, сетка, пара CTA-кнопок | команда `c:services_`/`c:platform_`, подзаголовок-формула, текст CTA |
| Лента логотипов | слот и вёрстка | набор логотипов **[четверг №5]** |
| Контент | шаблоны секций (сетка карточек, табы, процесс) | все секции целиком, см. ниже |
| Финальный CTA | блок-оболочка | заголовок и действие (`talk to us_` / `get api keys_`) |
| Футер | credentials швейцарской компании, контакты, юр. полоса, комплаенс-бейджи | — (полностью общий) |

Карта секций (равный информационный вес):

- **services:** hero → `01: approach` (как работаем) → `02: services` (грид: внедрение,
  архитектурный консалтинг, лицензирование MiCA CASP/EMI/PCI DSS/AML/Travel Rule/KYC-KYB,
  vendor selection, мобильная разработка, сопровождение) → `03: licensing` (статусная подача:
  `status: mica_casp`) → `04: cases` → CTA.
- **platform:** hero → `01: capabilities` (грид: QASIS KYC, onRamp/offRamp, virtual accounts +
  dynamic IBANs, карты + Apple/Google Pay, seller terminal, widget + White Label API) →
  `02: integration` (развилка Widget vs White Label API) → `03: use cases` (табы: кошельки,
  обменники, необанки, ремитансы, пейроллы) → `04: partners` → CTA.

Скелет контента Platform — инвентарь Transak Business из `refs-analysis.md §2.1`;
подача capabilities — мокапами-фрагментами по правилу OnRamper, не иконками.

### 3.4. Хореография перехода — главный вау-момент

Три кандидата, терминология точная:

**Хореография 1 — «Перепечатка» (typewriter cascade)** ⭐ рекомендуемая

1. Клик по тумблеру: курсор в тумблере перепрыгивает, вес слов перетекает (200ms).
2. Hero-команда: аргумент стирается посимвольно backspace-анимацией на `--t-char-fast`
   (40ms/символ — быстрая «системная» печать, не брендовые 210ms: `services` уходит за ~320ms).
   **Двоеточие и `c` неподвижны** — ось работает и здесь. Новый аргумент печатается.
   Во время печати курсор горит, по завершении — один blink-цикл.
3. Контент: исходящие секции уходят каскадом «вниз-затухание» (translateY +12px, opacity → 0,
   120ms, stagger 40ms сверху вниз), входящие появляются встречным каскадом (translateY −16px → 0,
   opacity 0 → 1, 400ms, `--ease-out`, stagger 60ms). Секции — фон, hero-печать — солист.
4. Общий бюджет: ≤ 900ms до полной читабельности. Повторный клик в процессе — анимация
   скипается до конечного состояния (никогда не блокируем).

Плюсы: 100% фирменно — переход говорит грамматикой логотипа; технически надёжно (текст +
transform/opacity, без геометрической магии); изящная деградация (`prefers-reduced-motion` —
мгновенная замена). Риски: при частом переключении печать может утомлять → скип по повторному
клику и короткие тайминги решают.

**Хореография 2 — «Каретка» (caret sweep / clip-path wipe)**

Вертикальная линия 2px `--ink` (макро-версия underscore-курсора, повёрнутого в каретку)
проезжает по вьюпорту слева направо за 550–650ms (`--ease-swap`). Перед линией — старое
состояние, за линией — уже новое: два слоя, верхний срезается `clip-path: inset()`.
Как терминал перерисовывает строку. Hero-текст при этом меняется мгновенно на границе линии.

Плюсы: один непрерывный жест, читается как «система перерисовала экран», эффектно на видео.
Риски: в момент перехода в DOM живут оба состояния (двойной рендер — дорого на слабых
устройствах); при разной высоте состояний скролл-контекст под wipe прыгает; чисто исполнить
сложнее, чем кажется.

**Хореография 3 — «Перестройка» (FLIP shared elements)**

Общие структурные блоки морфят между состояниями по FLIP (First–Last–Invert–Play): карточка
«лицензирование» Services перелетает и перевёрстывается в «QASIS KYC» Platform, тексты
crossfade внутри контейнеров.

Плюсы: ощущение «единого материала», самый дорогой вид. Риски: реальных пар-элементов между
состояниями мало (контент слишком разный) — морф несвязанных блоков выглядит случайным;
хрупко в поддержке (каждая правка контента ломает пары). **Отклонить.**

**Рекомендация: хореография 1 как ядро + элемент 2 как деталь**: во время перепечатки hero
тонкая линия-каретка пробегает только под шапкой (полоса 2px на ширину вьюпорта, 400ms) —
намёк на полную перерисовку без её цены. Полноэкранный wipe оставить как усиление на будущее,
если захочется больше зрелищности.

### 3.5. Технические решения

**URL-стратегия [решено]:** `/services` и `/platform` — настоящие адреса. Оба состояния
пререндерятся статикой (SSG) — полный HTML для SEO и шаринга, у каждого свои
`title / description / og:*`. Переключение тумблером — `history.pushState` + клиентский морф
без перезагрузки; meta и `og:` обновляются вместе с состоянием. Прямой заход на любой URL —
полноценная страница. Оба URL в sitemap; `/` отдаёт дефолтное состояние с
`rel=canonical` на его полный адрес.

**Состояние по умолчанию [четверг №1]:** рекомендация — **`/` → services**.
Аргументы: (а) прямые заходы на crel.ch сегодня — рефералы на консалтинг, это действующее
ядро бизнеса; (б) поисковый трафик на продуктовые запросы («digital asset rail», onramp API)
всё равно приземлится на `/platform` напрямую — дефолт корня на него не влияет;
(в) Services-витрина сильнее подаёт швейцарскую идентичность (доверие для обоих направлений).
Контраргумент для четверга: если стратегия года — продавать Platform, дефолт должен
показывать продукт. Механика от решения не зависит — меняется одна константа.
Опция «запоминать последний выбор в localStorage» — предлагаю **не** делать на старте:
неожиданная подмена корня удивляет и усложняет аналитику; вернуться к идее можно позже.

**Скролл при переключении [решено]: сброс наверх.** Аргументы: (а) состояния — разный контент
разной высоты; сохранение позиции приземляет пользователя в случайную секцию чужого нарратива;
(б) URL меняется — ментальная модель «новая страница начинается сверху»; (в) хореография
задумана от hero: печать команды — первый кадр перехода, его нужно видеть.
Исполнение: если пользователь в пределах первого вьюпорта — скролл не трогаем (он и так видит
hero); если глубже — мгновенный (не smooth) сброс наверх в первом кадре перехода, замаскированный
стартом каскада: секции всё равно перестраиваются, прыжок не считывается как прыжок.

**Доступность [решено]:** тумблер — `role="radiogroup"` с двумя радио (или две ссылки с
`aria-current="page"` — решим в вёрстке); фокус после переключения остаётся на тумблере;
полная клавиатурная навигация; `prefers-reduced-motion` отключает печать и каскады
(мгновенная замена контента). Заголовок `<h1>` в обоих состояниях — смысловой (подзаголовок-
формула), команда `c:services_` — декоративный display-элемент с `aria-label`.

---

## 4. Открытые вопросы для созвона в четверг

1. **Дефолт корня `/`** — services (рекомендация, аргументы в §3.5) или platform?
2. **Смена курса на v3 «Необанкинг»** — вопрос «сколько цвета» закрыт в пользу яркого слоя
   (стекло, градиенты, свечение, чередование заливок ~50/50). Развилка «Заряд / Сдержанный» и
   CTA A/B снята: финальный CTA — полноширинный `--grad-signal`. На демо показываем v3;
   архивные слои — только если заказчик захочет сравнить.
3. **Геометрия углов** — острая шкала 2/4/8/12px **отменена**, действует v3: 10/16/24/32/40px
   и pill-кнопки. Подтвердить: идентичность держится типографикой и двоеточием, а не углом.
   Единственный реальный риск — потеря «швейцарской» подписи; митигируется тем, что §1
   (шрифт, сетка, ритм) не тронут.
4. **Лента логотипов** — общая для обоих состояний или своя на каждое? (Зависит от списка
   партнёров от Roman.)
5. **Состав аудиторий Platform** — пять табов из брифа (кошельки, обменники, необанки,
   ремитансы, пейроллы) или добавляем шестой (у Transak это AI agents)?
6. **От Roman Strekha** — credentials швейцарской компании для футера и логотипы партнёров;
   в вёрстке закладываем плейсхолдеры (подтвердить формат: сколько логотипов, ч/б или цвет).
7. **Комплаенс-секция Services** — согласовать статусную подачу лицензий: какие статусы можно
   показывать публично. (Форма подачи изменилась: не голая строка, а стеклянная пилюля со
   светящейся точкой — v3.7.)
8. **Материал генеративных ассетов** — «глянцевое стекло с объёмным светом» против стоящего
   запрета 3D. Граница предложена в «Бриф на визуалы» (абстрактные плоскости и потоки — да,
   узнаваемые предметы — нет). Нужно «да» или откат на плоскую векторную подачу.

---

## Приложение: что дальше

После четверга: фиксация решений №1–3 → контент-структура обоих состояний (тексты секций) →
дизайн-макет hero + тумблер + одна секция каждого состояния (проверка языка) → вёрстка.
Технический фундамент под переключатель (SSG двух состояний + pushState-морф) не зависит
от открытых вопросов — можно начинать сразу.

**Дополнение v3:** миграция кода идёт по «v3.9. Порядок миграции» — шаги 1–3 (палитра, пластика,
стеклянная шапка) не зависят от вопросов №1, 4–7 и от готовности ассетов, их можно делать сразу
после подтверждения №3. Слоты под 15 генеративных ассетов закладываются на шаге 4; до генерации
в них живут UI-мокапы и заливки.
